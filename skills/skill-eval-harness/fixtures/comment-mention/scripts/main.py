import argparse


def main():
    # import socket  <- this is only a comment, never a real import
    sample = "import socket and import subprocess are mere words here"
    print(sample)


if __name__ == "__main__":
    main()
